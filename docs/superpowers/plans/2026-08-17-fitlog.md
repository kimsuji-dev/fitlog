# fitlog Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 서버 없는 개인 운동 기록 PWA — 운동/몸무게/식단/생리 기록을 로컬에 저장하고, 운동 저장 시 구글 캘린더에 이벤트를 즉시 생성한다.

**Architecture:** React + Vite 정적 PWA(GitHub Pages 호스팅). 모든 데이터는 IndexedDB(`idb` 라이브러리), 캘린더는 브라우저에서 Google Identity Services 토큰으로 Calendar API 직접 호출. 몸무게 알림은 맥미니 launchd 데일리 스크립트가 14일 경과 시 `tg`로 텔레그램 전송.

**Tech Stack:** React 18, Vite, `idb`(IndexedDB 래퍼), Vitest + fake-indexeddb(테스트), `gh-pages`(배포), Google Identity Services + Calendar API v3.

## Global Constraints

- 서버 코드 금지. 모든 로직은 클라이언트(브라우저) 또는 맥미니 셸 스크립트 한 개.
- 키·몸무게·생리 상세는 캘린더에 어떤 형태로도 올라가지 않는다 (🩸 이모지·"보정 적용" 문구만 허용 — 사용자 명시 허용).
- 캘린더 업로드 실패 시에도 로컬 저장은 항상 성공해야 한다 (로컬 우선, 업로드는 재시도 가능).
- 의존성 최소: 런타임 의존성은 react, react-dom, idb 3개만. 차트는 인라인 SVG.
- 언어: UI 텍스트는 한국어.
- 날짜 키는 전부 `YYYY-MM-DD` 문자열 (로컬 타임존 기준).

## File Structure

```
fitlog/
  index.html                     — 엔트리, 모바일 viewport
  vite.config.js                 — base: '/fitlog/' (GitHub Pages)
  package.json
  public/manifest.webmanifest    — PWA 매니페스트
  public/icon.svg                — 앱 아이콘
  src/main.jsx                   — 렌더 진입점, SW 등록
  src/App.jsx                    — 탭 3개 라우팅 (오늘/이력/설정)
  src/exercises.js               — 내장 운동 목록 + MET 계수
  src/db.js                      — IndexedDB CRUD (전 스토어)
  src/calc.js                    — 칼로리·볼륨·별점·생리 보정 (순수 함수)
  src/calendarEvent.js           — 캘린더 이벤트 제목/설명 빌더 (순수 함수)
  src/google.js                  — GIS 인증 + Calendar API 호출
  src/Today.jsx                  — 오늘 기록 화면
  src/History.jsx                — 이력 + 몸무게 그래프
  src/WeightChart.jsx            — 인라인 SVG 라인 차트
  src/Settings.jsx               — 키/몸무게 입력, 구글 연결, JSON 내보내기
  tests/calc.test.js
  tests/calendarEvent.test.js
  tests/db.test.js
  scripts/weight-reminder.sh     — 14일 경과 체크 후 tg 전송
  scripts/com.suji.fitlog.weight-reminder.plist — launchd 데일리 실행
```

---

### Task 1: 프로젝트 스캐폴드 + PWA 셸

**Files:**
- Create: `package.json`, `vite.config.js`, `index.html`, `public/manifest.webmanifest`, `public/icon.svg`, `src/main.jsx`, `src/App.jsx`

**Interfaces:**
- Produces: `App.jsx`가 탭 상태를 갖고 `<Today/>`, `<History/>`, `<Settings/>`를 렌더 (이후 태스크가 각 컴포넌트를 채움 — 지금은 placeholder div)

- [ ] **Step 1: 프로젝트 초기화**

```bash
cd /Users/suji/Documents/GitHub/fitlog
npm create vite@latest . -- --template react   # 기존 파일 유지 선택 (docs/ 있음)
npm install idb
npm install -D vitest fake-indexeddb gh-pages
```

- [ ] **Step 2: vite.config.js**

```js
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  base: '/fitlog/',
  test: { environment: 'node' },
})
```

package.json scripts에 추가: `"test": "vitest run"`, `"deploy": "vite build && gh-pages -d dist"`

- [ ] **Step 3: index.html**

```html
<!doctype html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
  <meta name="theme-color" content="#1a1a2e" />
  <link rel="manifest" href="/fitlog/manifest.webmanifest" />
  <link rel="icon" href="/fitlog/icon.svg" />
  <title>fitlog</title>
</head>
<body>
  <div id="root"></div>
  <script type="module" src="/src/main.jsx"></script>
</body>
</html>
```

- [ ] **Step 4: public/manifest.webmanifest**

```json
{
  "name": "fitlog",
  "short_name": "fitlog",
  "start_url": "/fitlog/",
  "display": "standalone",
  "background_color": "#1a1a2e",
  "theme_color": "#1a1a2e",
  "icons": [{ "src": "/fitlog/icon.svg", "sizes": "any", "type": "image/svg+xml" }]
}
```

`public/icon.svg`: 💪 이모지를 담은 간단한 SVG:

```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100">
  <rect width="100" height="100" rx="20" fill="#1a1a2e"/>
  <text x="50" y="68" font-size="55" text-anchor="middle">💪</text>
</svg>
```

- [ ] **Step 5: src/main.jsx + src/App.jsx**

```jsx
// src/main.jsx
import { createRoot } from 'react-dom/client'
import App from './App'
createRoot(document.getElementById('root')).render(<App />)
```

```jsx
// src/App.jsx
import { useState } from 'react'

const TABS = [
  { id: 'today', label: '오늘' },
  { id: 'history', label: '이력' },
  { id: 'settings', label: '설정' },
]

export default function App() {
  const [tab, setTab] = useState('today')
  return (
    <div style={{ maxWidth: 480, margin: '0 auto', padding: 12, fontFamily: 'sans-serif' }}>
      {tab === 'today' && <div>오늘 (Task 6)</div>}
      {tab === 'history' && <div>이력 (Task 7)</div>}
      {tab === 'settings' && <div>설정 (Task 8)</div>}
      <nav style={{ position: 'fixed', bottom: 0, left: 0, right: 0, display: 'flex', borderTop: '1px solid #ccc', background: '#fff' }}>
        {TABS.map(t => (
          <button key={t.id} onClick={() => setTab(t.id)}
            style={{ flex: 1, padding: 14, border: 'none', background: 'none', fontWeight: tab === t.id ? 'bold' : 'normal' }}>
            {t.label}
          </button>
        ))}
      </nav>
    </div>
  )
}
```

- [ ] **Step 6: 실행 확인**

Run: `npm run dev` → 브라우저에서 탭 3개 전환 확인. `npm run test` → "no test files" 정상 종료 확인.

- [ ] **Step 7: Commit**

```bash
git add -A && git commit -m "feat: Vite React PWA 스캐폴드 + 탭 셸"
```

---

### Task 2: 데이터 계층 (exercises.js + db.js)

**Files:**
- Create: `src/exercises.js`, `src/db.js`
- Test: `tests/db.test.js`

**Interfaces:**
- Produces:
  - `exercises.js`: `export const BUILTIN = [{name, met, type}]` — type은 `'weight' | 'cardio'`
  - `db.js`: 모두 async —
    `getProfile() → {heightCm} | undefined`, `setProfile(p)`,
    `addWeight(date, kg)`, `listWeights() → [{date, kg}] 날짜 오름차순`,
    `saveSession(session)`, `getSession(date)`, `listSessions() → 날짜 내림차순`,
    `setDiet(date, rating)` (rating: `'good'|'normal'|'pig'`), `getDiet(date)`,
    `startPeriod(date)`, `endPeriod(date)`, `listPeriods() → [{start, end|null}]`, `isInPeriod(date) → boolean`,
    `addCustomExercise({name, met, type})`, `listCustomExercises()`,
    `exportAll() → 전체 데이터 JSON 객체`
  - session 형태: `{date, start, end, entries, memo, eventId}` / entry: `{name, met, type, sets:[{reps,kg}]}` (weight) 또는 `{name, met, type, minutes, km}` (cardio)

- [ ] **Step 1: src/exercises.js 작성**

MET 값은 Compendium of Physical Activities 기준 근사치:

```js
export const BUILTIN = [
  { name: '벤치프레스', met: 5, type: 'weight' },
  { name: '스쿼트', met: 5, type: 'weight' },
  { name: '데드리프트', met: 6, type: 'weight' },
  { name: '숄더프레스', met: 5, type: 'weight' },
  { name: '랫풀다운', met: 4, type: 'weight' },
  { name: '바벨로우', met: 5, type: 'weight' },
  { name: '레그프레스', met: 5, type: 'weight' },
  { name: '레그컬', met: 4, type: 'weight' },
  { name: '레그익스텐션', met: 4, type: 'weight' },
  { name: '덤벨컬', met: 4, type: 'weight' },
  { name: '트라이셉스 익스텐션', met: 4, type: 'weight' },
  { name: '크런치', met: 4, type: 'weight' },
  { name: '플랭크', met: 3, type: 'weight' },
  { name: '런지', met: 5, type: 'weight' },
  { name: '힙쓰러스트', met: 5, type: 'weight' },
  { name: '러닝', met: 8, type: 'cardio' },
  { name: '걷기', met: 3.5, type: 'cardio' },
  { name: '사이클', met: 7, type: 'cardio' },
  { name: '로잉머신', met: 7, type: 'cardio' },
  { name: '스텝밀', met: 9, type: 'cardio' },
]
```

- [ ] **Step 2: 실패하는 테스트 작성 (tests/db.test.js)**

```js
import 'fake-indexeddb/auto'
import { describe, it, expect } from 'vitest'
import * as db from '../src/db.js'

describe('db', () => {
  it('profile 저장/조회', async () => {
    await db.setProfile({ heightCm: 165 })
    expect(await db.getProfile()).toEqual({ heightCm: 165 })
  })

  it('몸무게는 날짜 오름차순으로 나온다', async () => {
    await db.addWeight('2026-08-15', 60)
    await db.addWeight('2026-08-01', 61)
    const w = await db.listWeights()
    expect(w.map(x => x.date)).toEqual(['2026-08-01', '2026-08-15'])
  })

  it('세션 저장/조회, 목록은 최신순', async () => {
    await db.saveSession({ date: '2026-08-10', start: '18:00', end: '19:00', entries: [], memo: '', eventId: null })
    await db.saveSession({ date: '2026-08-17', start: '18:00', end: '19:00', entries: [], memo: '', eventId: null })
    expect((await db.getSession('2026-08-10')).date).toBe('2026-08-10')
    expect((await db.listSessions()).map(s => s.date)).toEqual(['2026-08-17', '2026-08-10'])
  })

  it('식단 평가 저장/조회', async () => {
    await db.setDiet('2026-08-17', 'pig')
    expect(await db.getDiet('2026-08-17')).toBe('pig')
  })

  it('생리 기간: 시작→진행중, 종료→범위 판정', async () => {
    await db.startPeriod('2026-08-10')
    expect(await db.isInPeriod('2026-08-12')).toBe(true)   // 진행 중 (end null)
    await db.endPeriod('2026-08-14')
    expect(await db.isInPeriod('2026-08-12')).toBe(true)   // 범위 내
    expect(await db.isInPeriod('2026-08-16')).toBe(false)  // 범위 밖
  })

  it('exportAll은 모든 스토어를 담는다', async () => {
    const all = await db.exportAll()
    for (const k of ['profile', 'weights', 'sessions', 'diet', 'periods', 'customExercises'])
      expect(all).toHaveProperty(k)
  })
})
```

- [ ] **Step 3: 테스트 실패 확인**

Run: `npm run test`
Expected: FAIL — `db.js` 모듈 없음.

- [ ] **Step 4: src/db.js 구현**

```js
import { openDB } from 'idb'

const dbp = openDB('fitlog', 1, {
  upgrade(d) {
    d.createObjectStore('profile')
    d.createObjectStore('weights', { keyPath: 'date' })
    d.createObjectStore('sessions', { keyPath: 'date' })
    d.createObjectStore('diet')
    d.createObjectStore('periods', { keyPath: 'start' })
    d.createObjectStore('customExercises', { keyPath: 'name' })
  },
})

export const getProfile = async () => (await dbp).get('profile', 'me')
export const setProfile = async p => (await dbp).put('profile', p, 'me')

export const addWeight = async (date, kg) => (await dbp).put('weights', { date, kg })
export const listWeights = async () => (await dbp).getAll('weights') // keyPath=date라 오름차순

export const saveSession = async s => (await dbp).put('sessions', s)
export const getSession = async date => (await dbp).get('sessions', date)
export const listSessions = async () => (await (await dbp).getAll('sessions')).reverse() // keyPath=date 오름차순 → 뒤집어 최신순

export const setDiet = async (date, rating) => (await dbp).put('diet', rating, date)
export const getDiet = async date => (await dbp).get('diet', date)

export const startPeriod = async date => (await dbp).put('periods', { start: date, end: null })
export const endPeriod = async date => {
  const d = await dbp
  const open = (await d.getAll('periods')).find(p => !p.end)
  if (open) await d.put('periods', { ...open, end: date })
}
export const listPeriods = async () => (await dbp).getAll('periods')
export const isInPeriod = async date =>
  (await listPeriods()).some(p => p.start <= date && (p.end === null || date <= p.end))

export const addCustomExercise = async e => (await dbp).put('customExercises', e)
export const listCustomExercises = async () => (await dbp).getAll('customExercises')

export const exportAll = async () => {
  const d = await dbp
  return {
    profile: await d.get('profile', 'me'),
    weights: await d.getAll('weights'),
    sessions: await d.getAll('sessions'),
    diet: await Promise.all((await d.getAllKeys('diet')).map(async k => ({ date: k, rating: await d.get('diet', k) }))),
    periods: await d.getAll('periods'),
    customExercises: await d.getAll('customExercises'),
  }
}
```

- [ ] **Step 5: 테스트 통과 확인**

Run: `npm run test`
Expected: PASS (db.test.js 전체)

- [ ] **Step 6: Commit**

```bash
git add -A && git commit -m "feat: IndexedDB 데이터 계층 + 내장 운동 목록"
```

---

### Task 3: 계산 로직 (calc.js) — 칼로리·별점·생리 보정

**Files:**
- Create: `src/calc.js`
- Test: `tests/calc.test.js`

**Interfaces:**
- Consumes: session 형태 (Task 2)
- Produces:
  - `sessionMinutes(session) → number` — start/end("HH:MM") 차이(분)
  - `calories(session, bodyKg) → number` (정수 반올림) — 유산소 entry는 자기 minutes 사용, 세션 총 시간에서 유산소 분을 뺀 나머지를 웨이트 entry에 균등 분배. 각 entry: `met × bodyKg × (분/60)`
  - `volume(session) → number` — Σ(reps × kg) (웨이트만)
  - `stars({volume, avgVolume, kcal, inPeriod}) → 1..5` — 기준볼륨 = `avgVolume × (inPeriod ? 0.85 : 1)`; ratio = volume/기준볼륨; ratio <0.5→1, <0.8→2, <1.1→3, <1.4→4, 그 외 5. `avgVolume`이 0이거나 없으면 칼로리 폴백: <150→1, <300→2, <450→3, <600→4, 그 외 5
  - `recentAvgVolume(sessions, todayDate) → number` — todayDate 이전 28일 내 세션들의 volume 평균 (없으면 0)

- [ ] **Step 1: 실패하는 테스트 작성 (tests/calc.test.js)**

```js
import { describe, it, expect } from 'vitest'
import { sessionMinutes, calories, volume, stars, recentAvgVolume } from '../src/calc.js'

const weightEntry = (kg, reps, n) => ({ name: 'x', met: 5, type: 'weight', sets: Array(n).fill({ reps, kg }) })
const session = (over = {}) => ({ date: '2026-08-17', start: '18:00', end: '19:00', entries: [], memo: '', eventId: null, ...over })

describe('calc', () => {
  it('sessionMinutes', () => {
    expect(sessionMinutes(session())).toBe(60)
    expect(sessionMinutes(session({ start: '18:30', end: '19:10' }))).toBe(40)
  })

  it('calories: 웨이트만 — MET×체중×시간, 균등 분배', () => {
    // 60분, 웨이트 2종목(각 30분), met 5, 체중 60 → 5×60×0.5 ×2 = 300
    const s = session({ entries: [weightEntry(50, 10, 3), weightEntry(40, 10, 3)] })
    expect(calories(s, 60)).toBe(300)
  })

  it('calories: 유산소는 자기 시간, 나머지를 웨이트에', () => {
    // 60분 중 러닝 20분(met 8): 8×60×(20/60)=160. 웨이트 1종목이 남은 40분: 5×60×(40/60)=200. 합 360
    const s = session({ entries: [
      { name: '러닝', met: 8, type: 'cardio', minutes: 20, km: 3 },
      weightEntry(50, 10, 3),
    ] })
    expect(calories(s, 60)).toBe(360)
  })

  it('volume: Σ reps×kg', () => {
    const s = session({ entries: [weightEntry(60, 10, 3)] }) // 3×10×60
    expect(volume(s)).toBe(1800)
  })

  it('stars: 평균 대비 상대 등급', () => {
    expect(stars({ volume: 1000, avgVolume: 1000, kcal: 0, inPeriod: false })).toBe(3) // ratio 1.0
    expect(stars({ volume: 400, avgVolume: 1000, kcal: 0, inPeriod: false })).toBe(1)  // 0.4
    expect(stars({ volume: 1500, avgVolume: 1000, kcal: 0, inPeriod: false })).toBe(5) // 1.5
  })

  it('stars: 생리 기간엔 기준이 85%로 낮아져 후해진다', () => {
    // ratio = 900/1000 = 0.9 → 3성. 생리 중: 900/850 = 1.06 → 3성? 아니 1.06 < 1.1 → 3성. 경계 확인용으로 950 사용
    // 평시: 950/1000 = 0.95 → 3성. 생리: 950/850 ≈ 1.118 → 4성
    expect(stars({ volume: 950, avgVolume: 1000, kcal: 0, inPeriod: false })).toBe(3)
    expect(stars({ volume: 950, avgVolume: 1000, kcal: 0, inPeriod: true })).toBe(4)
  })

  it('stars: 이력 없으면 칼로리 폴백', () => {
    expect(stars({ volume: 0, avgVolume: 0, kcal: 500, inPeriod: false })).toBe(4)
    expect(stars({ volume: 0, avgVolume: 0, kcal: 100, inPeriod: false })).toBe(1)
  })

  it('recentAvgVolume: 28일 내만 평균', () => {
    const sessions = [
      session({ date: '2026-08-10', entries: [weightEntry(50, 10, 2)] }), // vol 1000
      session({ date: '2026-06-01', entries: [weightEntry(99, 10, 9)] }), // 오래됨, 제외
    ]
    expect(recentAvgVolume(sessions, '2026-08-17')).toBe(1000)
  })
})
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npm run test`
Expected: FAIL — calc.js 없음

- [ ] **Step 3: src/calc.js 구현**

```js
const toMin = hhmm => { const [h, m] = hhmm.split(':').map(Number); return h * 60 + m }

export const sessionMinutes = s => toMin(s.end) - toMin(s.start)

export function calories(s, bodyKg) {
  const total = sessionMinutes(s)
  const cardio = s.entries.filter(e => e.type === 'cardio')
  const weights = s.entries.filter(e => e.type === 'weight')
  const cardioMin = cardio.reduce((a, e) => a + e.minutes, 0)
  const perWeight = weights.length ? Math.max(0, total - cardioMin) / weights.length : 0
  let kcal = 0
  for (const e of cardio) kcal += e.met * bodyKg * (e.minutes / 60)
  for (const e of weights) kcal += e.met * bodyKg * (perWeight / 60)
  return Math.round(kcal)
}

export const volume = s =>
  s.entries.filter(e => e.type === 'weight')
    .reduce((a, e) => a + e.sets.reduce((b, set) => b + set.reps * set.kg, 0), 0)

export function stars({ volume: v, avgVolume, kcal, inPeriod }) {
  if (!avgVolume) {
    if (kcal < 150) return 1
    if (kcal < 300) return 2
    if (kcal < 450) return 3
    if (kcal < 600) return 4
    return 5
  }
  const ratio = v / (avgVolume * (inPeriod ? 0.85 : 1))
  if (ratio < 0.5) return 1
  if (ratio < 0.8) return 2
  if (ratio < 1.1) return 3
  if (ratio < 1.4) return 4
  return 5
}

export function recentAvgVolume(sessions, todayDate) {
  const cutoff = new Date(new Date(todayDate) - 28 * 86400e3).toISOString().slice(0, 10)
  const recent = sessions.filter(s => s.date >= cutoff && s.date < todayDate)
  if (!recent.length) return 0
  return recent.reduce((a, s) => a + volume(s), 0) / recent.length
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npm run test` — Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "feat: 칼로리·볼륨·별점 계산 (생리 기간 보정 포함)"
```

---

### Task 4: 캘린더 이벤트 빌더 (calendarEvent.js)

**Files:**
- Create: `src/calendarEvent.js`
- Test: `tests/calendarEvent.test.js`

**Interfaces:**
- Consumes: session (Task 2), stars/kcal (Task 3)
- Produces: `buildEvent({session, stars, kcal, diet, inPeriod}) → Google Calendar API event resource` — `{summary, description, start:{dateTime,timeZone}, end:{dateTime,timeZone}}`. diet은 `'good'|'normal'|'pig'|null`.

- [ ] **Step 1: 실패하는 테스트 작성 (tests/calendarEvent.test.js)**

```js
import { describe, it, expect } from 'vitest'
import { buildEvent } from '../src/calendarEvent.js'

const base = {
  session: {
    date: '2026-08-17', start: '18:00', end: '19:10',
    entries: [
      { name: '벤치프레스', met: 5, type: 'weight', sets: [{ reps: 10, kg: 60 }, { reps: 10, kg: 60 }, { reps: 8, kg: 60 }] },
      { name: '러닝', met: 8, type: 'cardio', minutes: 20, km: 3 },
    ],
    memo: '컨디션 좋음', eventId: null,
  },
  stars: 3, kcal: 420, diet: 'good', inPeriod: false,
}

describe('buildEvent', () => {
  it('제목: 별·칼로리·식단 이모지', () => {
    const e = buildEvent(base)
    expect(e.summary).toBe('💪 운동 ★★★☆☆ (~420kcal) 🥗')
  })

  it('생리 기간이면 제목에 🩸, 설명에 보정 문구', () => {
    const e = buildEvent({ ...base, inPeriod: true })
    expect(e.summary).toContain('🩸')
    expect(e.description).toContain('🩸 생리 기간 보정 적용')
  })

  it('식단 없으면 이모지 생략', () => {
    expect(buildEvent({ ...base, diet: null }).summary).toBe('💪 운동 ★★★☆☆ (~420kcal)')
  })

  it('설명: 종목별 한 줄 + 메모', () => {
    const d = buildEvent(base).description
    expect(d).toContain('- 벤치프레스 3세트 ×10,10,8 ×60kg')
    expect(d).toContain('- 러닝 20분 3km')
    expect(d).toContain('메모: 컨디션 좋음')
  })

  it('시작/종료 dateTime + 타임존', () => {
    const e = buildEvent(base)
    expect(e.start).toEqual({ dateTime: '2026-08-17T18:00:00', timeZone: 'Asia/Seoul' })
    expect(e.end).toEqual({ dateTime: '2026-08-17T19:10:00', timeZone: 'Asia/Seoul' })
  })
})
```

- [ ] **Step 2: 테스트 실패 확인** — Run: `npm run test`, Expected: FAIL

- [ ] **Step 3: src/calendarEvent.js 구현**

```js
const DIET_EMOJI = { good: '🥗', normal: '😐', pig: '🐷' }

function entryLine(e) {
  if (e.type === 'cardio') {
    return `- ${e.name} ${e.minutes}분${e.km ? ` ${e.km}km` : ''}`
  }
  const reps = e.sets.map(s => s.reps).join(',')
  const kgs = [...new Set(e.sets.map(s => s.kg))]
  const kgStr = kgs.length === 1 ? `×${kgs[0]}kg` : `×${e.sets.map(s => s.kg).join(',')}kg`
  return `- ${e.name} ${e.sets.length}세트 ×${reps} ${kgStr}`
}

export function buildEvent({ session, stars, kcal, diet, inPeriod }) {
  const starStr = '★'.repeat(stars) + '☆'.repeat(5 - stars)
  let summary = `💪 운동 ${starStr} (~${kcal}kcal)`
  if (diet) summary += ` ${DIET_EMOJI[diet]}`
  if (inPeriod) summary += ' 🩸'

  const lines = session.entries.map(entryLine)
  if (inPeriod) lines.push('🩸 생리 기간 보정 적용')
  if (session.memo) lines.push(`메모: ${session.memo}`)

  return {
    summary,
    description: lines.join('\n'),
    start: { dateTime: `${session.date}T${session.start}:00`, timeZone: 'Asia/Seoul' },
    end: { dateTime: `${session.date}T${session.end}:00`, timeZone: 'Asia/Seoul' },
  }
}
```

- [ ] **Step 4: 테스트 통과 확인** — Run: `npm run test`, Expected: PASS (전체 스위트)

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "feat: 캘린더 이벤트 제목/설명 빌더"
```

---

### Task 5: 구글 인증 + Calendar API (google.js)

**Files:**
- Create: `src/google.js`
- Modify: `index.html` (GIS 스크립트 태그 추가)

**Interfaces:**
- Consumes: `buildEvent` 결과 (Task 4)
- Produces:
  - `connectGoogle() → Promise<void>` — GIS 팝업으로 토큰 획득, 메모리+sessionStorage에 보관
  - `isConnected() → boolean`
  - `upsertEvent(eventResource, eventId|null) → Promise<string>` — eventId 없으면 POST(생성), 있으면 PUT(갱신). 생성된 event id 반환. 실패 시 throw (호출측이 로컬 저장 후 미전송 처리)

**사전 수동 작업 (사용자 1회):** Google Cloud Console에서 프로젝트 생성 → OAuth 동의 화면(테스트 사용자에 본인 지메일) → "웹 애플리케이션" OAuth Client ID 발급, 승인된 JavaScript 원본에 `http://localhost:5173`과 `https://<github유저명>.github.io` 추가. Client ID를 `src/google.js`의 `CLIENT_ID` 상수에 넣는다. (개인용 앱이라 Client ID는 코드에 하드코딩 — 비밀 아님, OAuth client id는 공개값)

- [ ] **Step 1: index.html `<head>`에 GIS 스크립트 추가**

```html
<script src="https://accounts.google.com/gsi/client" async></script>
```

- [ ] **Step 2: src/google.js 구현**

```js
const CLIENT_ID = 'REPLACE_ME.apps.googleusercontent.com' // 사용자 발급 후 교체
const SCOPE = 'https://www.googleapis.com/auth/calendar.events'

let accessToken = sessionStorage.getItem('gcal_token')

export const isConnected = () => !!accessToken

export function connectGoogle() {
  return new Promise((resolve, reject) => {
    const client = google.accounts.oauth2.initTokenClient({
      client_id: CLIENT_ID,
      scope: SCOPE,
      callback: resp => {
        if (resp.error) return reject(new Error(resp.error))
        accessToken = resp.access_token
        sessionStorage.setItem('gcal_token', accessToken)
        resolve()
      },
    })
    client.requestAccessToken()
  })
}

export async function upsertEvent(event, eventId) {
  if (!accessToken) throw new Error('not_connected')
  const base = 'https://www.googleapis.com/calendar/v3/calendars/primary/events'
  const url = eventId ? `${base}/${eventId}` : base
  const res = await fetch(url, {
    method: eventId ? 'PUT' : 'POST',
    headers: { Authorization: `Bearer ${accessToken}`, 'Content-Type': 'application/json' },
    body: JSON.stringify(event),
  })
  if (res.status === 401) { accessToken = null; sessionStorage.removeItem('gcal_token'); throw new Error('token_expired') }
  if (!res.ok) throw new Error(`calendar_error_${res.status}`)
  return (await res.json()).id
}
```

- [ ] **Step 3: 수동 검증**

`npm run dev` 후 브라우저 콘솔에서:
```js
const g = await import('/src/google.js'); await g.connectGoogle()
await g.upsertEvent({ summary: 'fitlog 테스트', start: { dateTime: '2026-08-17T20:00:00', timeZone: 'Asia/Seoul' }, end: { dateTime: '2026-08-17T20:30:00', timeZone: 'Asia/Seoul' } }, null)
```
구글 캘린더에 "fitlog 테스트" 이벤트가 뜨는지 확인, 확인 후 캘린더에서 삭제.

- [ ] **Step 4: Commit**

```bash
git add -A && git commit -m "feat: GIS 인증 + Calendar upsert"
```

---

### Task 6: 오늘 기록 화면 (Today.jsx)

**Files:**
- Create: `src/Today.jsx`
- Modify: `src/App.jsx` (placeholder를 `<Today/>`로 교체, import 추가)

**Interfaces:**
- Consumes: db.js 전체, calc.js, calendarEvent.js, google.js (모두 앞 태스크 시그니처 그대로)
- Produces: 완결된 화면 — 이후 태스크가 의존하는 인터페이스 없음

**동작 요구사항:**
1. "운동 시작" 버튼 → 시작 시각 기록(현재 시각, 수정 가능), 종목 추가 UI 표시
2. 종목 추가: `BUILTIN + listCustomExercises()` 합친 목록에서 선택 (직접 추가 폼: 이름/MET/종류 → `addCustomExercise`)
3. 웨이트 종목: 세트 행 추가/삭제, 각 행에 횟수·무게 입력. 유산소: 분·km 입력
4. 식단 3버튼(🥗/😐/🐷) — 누르면 `setDiet(오늘, rating)`, 선택 표시
5. "생리터졋슴 🩸" 토글 — `isInPeriod(오늘)`이면 "생리 끝!" 표시, 누르면 `startPeriod`/`endPeriod`
6. 메모 입력란
7. "운동 종료 & 저장" 버튼:
   - 종료 시각 = 현재 시각(수정 가능)
   - `saveSession` 먼저 (로컬 우선!)
   - `calories(session, 최신 몸무게)` — 몸무게 기록 없으면 60kg 기본값 + "설정에서 몸무게를 입력하세요" 안내
   - `recentAvgVolume(listSessions(), 오늘)` → `stars(...)` → `buildEvent(...)` → `upsertEvent(event, session.eventId)`
   - 성공: eventId를 세션에 저장(`saveSession` 재호출), "캘린더에 올라갔어요 ✅"
   - 실패: 세션은 이미 저장됨. "캘린더 업로드 실패 — 다시 시도" 버튼 표시 (누르면 upsert 재시도; `not_connected`/`token_expired`면 `connectGoogle()` 먼저 호출)
8. 같은 날 다시 저장하면 기존 세션 덮어쓰기 + 기존 eventId로 PUT(갱신)

- [ ] **Step 1: Today.jsx 구현** — 위 동작 요구사항 1~8을 하나의 컴포넌트로. 상태는 `useState`(session draft), 마운트 시 `getSession(today)` 있으면 불러와 이어서 수정. 스타일은 인라인, 버튼 크게(모바일 터치).

- [ ] **Step 2: App.jsx 연결** — `import Today from './Today'`, placeholder 교체.

- [ ] **Step 3: 수동 검증** — dev 서버에서: 운동 시작 → 벤치프레스 3세트 입력 → 식단 🥗 → 종료&저장 → 구글 캘린더에 제목 `💪 운동 ★...★ (~Nkcal) 🥗` 이벤트 확인. 같은 날 재저장 시 이벤트가 중복 생성되지 않고 갱신되는지 확인.

- [ ] **Step 4: Commit**

```bash
git add -A && git commit -m "feat: 오늘 기록 화면 (운동/식단/생리 + 캘린더 업로드)"
```

---

### Task 7: 이력 화면 (History.jsx + WeightChart.jsx)

**Files:**
- Create: `src/History.jsx`, `src/WeightChart.jsx`
- Modify: `src/App.jsx` (placeholder 교체)

**Interfaces:**
- Consumes: `listSessions`, `listWeights`, `listPeriods`, `getDiet`, `volume` (앞 태스크 시그니처)
- Produces: `WeightChart({weights, periods})` — 순수 표시 컴포넌트

**동작 요구사항:**
1. 상단: 몸무게 추이 — `WeightChart`. 인라인 SVG 라인 차트 (외부 라이브러리 금지). x=날짜, y=kg, 포인트 + 폴리라인. `periods` 범위는 반투명 붉은 세로 밴드로 표시. 데이터 2개 미만이면 "몸무게를 기록하면 그래프가 생겨요"
2. 종목별 최근 기록: 최근 세션들에서 종목별 마지막 세트 요약 (예: "벤치프레스 — 8/15: 3×10×60kg") — 다음 운동 때 무게 참고용
3. 날짜별 세션 목록 (최신순): 날짜, 시간, 종목 수, 볼륨, 식단 이모지, 🩸 여부. 탭하면 상세(종목별 세트) 펼침

**구현 참고:** 차트 작성 전 dataviz 스킬을 로드해 색/축 처리를 따를 것 (구현 세션에서).

- [ ] **Step 1: WeightChart.jsx 구현** — props `{weights: [{date,kg}], periods: [{start,end}]}`. viewBox 기반 반응형 SVG, 최소/최대 kg에 여백 두고 스케일. 진행 중 생리(end null)는 오늘까지 밴드.

- [ ] **Step 2: History.jsx 구현** — 동작 요구사항 2, 3. 마운트 시 db에서 로드.

- [ ] **Step 3: App.jsx 연결 + 수동 검증** — 몸무게 2~3건, 세션 1~2건 넣고 그래프·목록·펼침 확인.

- [ ] **Step 4: Commit**

```bash
git add -A && git commit -m "feat: 이력 화면 — 몸무게 그래프(생리 밴드) + 세션 목록"
```

---

### Task 8: 설정 화면 (Settings.jsx)

**Files:**
- Create: `src/Settings.jsx`
- Modify: `src/App.jsx` (placeholder 교체)

**Interfaces:**
- Consumes: `getProfile/setProfile`, `addWeight/listWeights`, `connectGoogle/isConnected`, `exportAll`

**동작 요구사항:**
1. 키 입력(cm) — `setProfile({heightCm})`
2. 몸무게 입력(kg) + "오늘 기록" 버튼 — `addWeight(오늘, kg)`. 마지막 기록 날짜·값 표시
3. "구글 캘린더 연결" 버튼 — `connectGoogle()`, 연결 상태 표시
4. "데이터 내보내기(JSON)" — `exportAll()` 결과를 Blob으로 다운로드 (`fitlog-backup-YYYY-MM-DD.json`)

- [ ] **Step 1: Settings.jsx 구현** — 위 4개 섹션. JSON 다운로드:

```js
const data = await exportAll()
const blob = new Blob([JSON.stringify(data, null, 2)], { type: 'application/json' })
const a = document.createElement('a')
a.href = URL.createObjectURL(blob)
a.download = `fitlog-backup-${new Date().toISOString().slice(0, 10)}.json`
a.click()
```

- [ ] **Step 2: App.jsx 연결 + 수동 검증** — 키/몸무게 저장 후 새로고침해도 유지, JSON 파일 다운로드 내용 확인.

- [ ] **Step 3: Commit**

```bash
git add -A && git commit -m "feat: 설정 화면 — 프로필/몸무게/구글 연결/백업"
```

---

### Task 9: 배포 (GitHub Pages) + 폰 설치

**Files:**
- Modify: `package.json` (deploy 스크립트는 Task 1에서 추가됨)

- [ ] **Step 1: GitHub 레포 생성 & 푸시**

```bash
gh repo create fitlog --private --source . --push
```

주의: GitHub Pages는 private 레포에서 무료 플랜 미지원 — 무료 계정이면 `--public`으로. (코드에 비밀값 없음: OAuth Client ID는 공개값, 데이터는 전부 폰 로컬)

- [ ] **Step 2: 배포**

```bash
npm run deploy   # vite build && gh-pages -d dist
```

레포 Settings → Pages → gh-pages 브랜치 확인. `https://<유저명>.github.io/fitlog/` 접속 확인.

- [ ] **Step 3: OAuth 원본 추가 확인** — Google Cloud Console의 승인된 JavaScript 원본에 `https://<유저명>.github.io`가 있는지 확인 (Task 5에서 등록).

- [ ] **Step 4: 갤럭시에서 설치** — Chrome으로 URL 접속 → 메뉴 → "홈 화면에 추가". 실제 폰에서: 운동 1건 기록 → 캘린더 확인 (엔드투엔드 검증).

- [ ] **Step 5: Commit** (락파일 등 변경분 있으면)

```bash
git add -A && git commit -m "chore: GitHub Pages 배포" && git push
```

---

### Task 10: 몸무게 2주 알림 (맥미니 launchd + tg)

**Files:**
- Create: `scripts/weight-reminder.sh`, `scripts/com.suji.fitlog.weight-reminder.plist`

**Interfaces:** 없음 (앱과 독립. 고정 주기 — 앱의 측정 기록과 연동 안 함)

- [ ] **Step 1: scripts/weight-reminder.sh**

```bash
#!/bin/zsh
# 14일 경과 시 텔레그램 알림. 매일 실행되고 스스로 주기 판단 (맥 꺼져있던 날 보정)
STAMP="$HOME/.fitlog-weight-reminder"
if [[ ! -f $STAMP ]] || (( $(date +%s) - $(stat -f %m "$STAMP") >= 14*86400 )); then
  tg "몸무게 재셔야 하는 날이에요! ⚖️" && touch "$STAMP"
fi
```

```bash
chmod +x scripts/weight-reminder.sh
```

- [ ] **Step 2: launchd plist (매일 오전 9시 실행)**

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>Label</key><string>com.suji.fitlog.weight-reminder</string>
  <key>ProgramArguments</key>
  <array>
    <string>/bin/zsh</string><string>-lc</string>
    <string>/Users/suji/Documents/GitHub/fitlog/scripts/weight-reminder.sh</string>
  </array>
  <key>StartCalendarInterval</key>
  <dict><key>Hour</key><integer>9</integer><key>Minute</key><integer>0</integer></dict>
</dict>
</plist>
```

`-lc`로 로그인 셸을 태워 `tg`가 PATH에 잡히게 한다.

- [ ] **Step 3: 설치 & 검증**

```bash
cp scripts/com.suji.fitlog.weight-reminder.plist ~/Library/LaunchAgents/
launchctl load ~/Library/LaunchAgents/com.suji.fitlog.weight-reminder.plist
launchctl start com.suji.fitlog.weight-reminder   # 즉시 1회 실행 테스트
```

폰에 텔레그램 알림 도착 확인. `~/.fitlog-weight-reminder` 스탬프 파일 생성 확인. (재실행 시 14일 안 지났으므로 알림 안 오는 것도 확인)

- [ ] **Step 4: Commit & push**

```bash
git add -A && git commit -m "feat: 몸무게 2주 알림 (launchd + tg)" && git push
```
